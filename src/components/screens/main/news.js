/**
 * @format
 */
import React from 'react';
import {
    FlatList,
    StyleSheet,
    Text,
    TouchableHighlight,
    View
} from 'react-native';

const NewsScreen = props => {
    return (
        <FlatList />
    );
};

export default NewsScreen;
